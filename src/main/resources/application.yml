spring:
  application:
    name: feb25-jpa
  datasource:
    url: jdbc:h2:./appdb
    username: sa
    password: sa123

  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate

    show-sql: true
